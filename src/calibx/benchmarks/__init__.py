"""Dataset-specific protocols kept outside the Calib-X core pipeline.

The core package estimates one pose per frame.  Modules below this namespace
own only paper benchmark protocol, manifests, and aggregation conventions.
"""
