# Paper reporting scripts

Table and figure generators consume compact, public summaries, manifests, and
selection records.  They never read a server `outputs/` directory directly and
never bundle raw dataset images by default.
