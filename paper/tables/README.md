# Table sources

This directory will contain compact, public table inputs and generated LaTeX
only after a clean protocol run reproduces the corresponding result.  Historic
server summaries are evidence for migration, not publishable table sources.
