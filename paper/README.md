# Compact paper artifacts

This directory stores only compact public inputs such as frame manifests,
provenance records, table sources, and figure-selection metadata.  Raw data,
model weights, experiment outputs, and rendered PNGs remain outside Git.
